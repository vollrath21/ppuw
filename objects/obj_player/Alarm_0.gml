/// @description CPU attack choice randomizer
if (round(random(1)) == 1) {
	keyboard_key_press(vk_divide);
	keyboard_key_release(vk_divide);

	
} else {
	keyboard_key_press(vk_backspace);
	keyboard_key_release(vk_backspace);
}